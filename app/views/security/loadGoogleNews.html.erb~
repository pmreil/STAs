
<%= @feed.inspect  %>

<% if @feed.nil? %>

  <strong>Sorry, no tweet results were found for ${$ticker|escape}</strong>

<% else %>
	<% @feed.entries.each do |entry| %>
    <div class="item"><%= entry.summary.html_safe %></div>
  <% end %>
<% end %>

